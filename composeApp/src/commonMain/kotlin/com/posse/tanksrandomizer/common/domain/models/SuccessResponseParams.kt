package com.posse.tanksrandomizer.common.domain.models

enum class SuccessResponseParams(val value: String) {
    ACCESS_TOKEN("access_token"),
    NICKNAME("nickname"),
    ACCOUNT_ID("account_id"),
    EXPIRES_AT("expires_at"),
}